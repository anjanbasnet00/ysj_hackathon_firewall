# data + models are big / generated — never commit
data/raw/*
data/interim/*
data/processed/*
models/*.pkl
!data/raw/.gitkeep
!data/processed/.gitkeep

# google drive oauth secrets — NEVER commit
credentials.json
token.json

# python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# local env
.env
